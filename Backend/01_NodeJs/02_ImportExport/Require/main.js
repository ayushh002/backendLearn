const {sum,sub,mul} = require('./calculator');

sum(6,4);
sub(6,4);
mul(6,4);

// It Follows the "index.js convention" — when we require('./calculator'), Node.js automatically
//  looks for index.js inside that folder.

